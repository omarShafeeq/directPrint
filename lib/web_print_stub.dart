import 'dart:typed_data';

Future<bool> silentPrintPdf(Uint8List pdfBytes) async {
  throw UnsupportedError('silentPrintPdf is only available on Flutter web.');
}

Future<bool> silentPrintPdfFromUrl(String url) async {
  throw UnsupportedError('silentPrintPdfFromUrl is only available on Flutter web.');
}
